[build]
builder = "nixpacks"
builderSettings = { nixpacksConfigPath = "" }

[deploy]
numReplicas = 1
restartPolicyType = "on-failure"

# 定时同步任务（每6小时一次）
[[cron]]
command = "npm run sync"
schedule = "0 */6 * * *"